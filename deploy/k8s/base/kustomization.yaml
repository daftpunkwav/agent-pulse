apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: agentpulse

resources:
  - backend.yaml
  - postgres.yaml
  - clickhouse.yaml
  - chroma.yaml
  - web.yaml
  - ingress.yaml
  - networkpolicy.yaml

commonLabels:
  app.kubernetes.io/part-of: agentpulse
  app.kubernetes.io/managed-by: kustomize
